/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */
package control;

import java.util.ArrayList;
import java.util.List;


/**
 *
 * @author Helio
 */
public class COProntuario {
    /*
    public static void addProntuario(VOPaciente pac, String sintomas, String diagnostico, String tratamento) {
        VOProntuario p = new VOProntuario();
        p.setPaciente(pac);
        p.setSintomas(sintomas);
        p.setDiagnostico(diagnostico);
        p.setTratamento(tratamento);
        
        DataBase.getListaProntuarios().add(p);
    }
    
    public static void delProntuario(VOProntuario p) {
        DataBase.getListaProntuarios().remove(p);
    }
    
    public static void attProntuario(VOProntuario p, VOPaciente pac, String sintomas, String diagnostico, String tratamento) {
        p.setDiagnostico(diagnostico);
        p.setPaciente(pac);
        p.setSintomas(sintomas);
        p.setTratamento(tratamento);
    }
    
    public static List<VOProntuario> listProntuario() {
        return DataBase.getListaProntuarios();
    }
    
    public static List<VOProntuario> listProntuario(VOPaciente paciente) {
        List<VOProntuario> ret = new ArrayList<>();
        
        for(VOProntuario p : DataBase.getListaProntuarios()) {
            if(p.getPaciente().equals(paciente)) {
                ret.add(p);
            }
        }
        
        return ret;
    }
  */  
}
