/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */
package control;

import java.util.ArrayList;
import java.util.Calendar;
import java.util.List;


/**
 *
 * @author Helio
 */
public class CORelatorioConsulta {
    /*
    public static List<VOConsulta> consultasDoDiaSeguinte() {
        Calendar amanha = Calendar.getInstance();
        Calendar dt = Calendar.getInstance();
        List<VOConsulta> retorno = new ArrayList<>();
        
        amanha.add(Calendar.DAY_OF_MONTH, 1);
        for(VOConsulta con : DataBase.getListaConsultas()) {
            dt.setTime(con.getDataHora());
            if(dt.get(Calendar.DAY_OF_MONTH) == amanha.get(Calendar.DAY_OF_MONTH)) {
                retorno.add(con);
            }
        }
        
        return retorno;
    }
    
    public static List<VOConsulta> consultasDoDiaSeguinte(boolean temEmailOuCelular) {
        Calendar amanha = Calendar.getInstance();
        Calendar dt = Calendar.getInstance();
        List<VOConsulta> retorno = new ArrayList<>();
        
        amanha.add(Calendar.DAY_OF_MONTH, 1);
        for(VOConsulta con : DataBase.getListaConsultas()) {
            dt.setTime(con.getDataHora());
            if(dt.get(Calendar.DAY_OF_MONTH) == amanha.get(Calendar.DAY_OF_MONTH)) {
                if(temEmailOuCelular && !(con.getPaciente().getEmail().equals("") && con.getPaciente().getCelular().equals(""))) {
                    retorno.add(con);
                }else if(!temEmailOuCelular && (con.getPaciente().getEmail().equals("") && con.getPaciente().getCelular().equals(""))) {
                    retorno.add(con);
                }
            }
        }
        
        return retorno;
    }
   */ 
}
